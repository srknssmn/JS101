let app = require('./employee') // JS veya KLASÖR İSMİNİ ÇAĞIRABİLİR. // index.js'den çekiyor.

// app('Serkan');
// app.sayName('Serkan');  // MODÜLÜ DEĞİŞKENE Func. VEREREK ÇAĞIRABİLİYORUZ.

app.sayName('Serkan')
app.sayAge(36)